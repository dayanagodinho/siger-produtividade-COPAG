{
  "name": "escala-hibrida",
  "version": "1.8.0",
  "description": "Controle de escala de trabalho híbrido (presencial / à distância / afastamento) com alerta de cobertura",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node scripts/migrate.js && node scripts/seed.js --so-se-vazio && node server.js",
    "dev": "node --watch server.js",
    "build": "echo 'Escala Hibrida nao tem etapa de build (HTML + JS puro).'",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "test": "node --test \"test/unit/**/*.test.js\"",
    "test:banco": "node --test \"test/banco/**/*.test.js\"",
    "start:so-servidor": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  }
}
